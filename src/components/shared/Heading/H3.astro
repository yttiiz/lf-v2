---
import { cn } from "@heroui/react";

type Props = {
	children: string;
	className?: string;
};

const { className, children } = Astro.props;
---

<h3 class={cn("text-xl/6 font-medium text-primary", className)}>
	{children}
</h3>
