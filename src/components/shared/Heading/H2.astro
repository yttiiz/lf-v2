---
import { cn } from "@heroui/react";

type Props = {
	children: string;
	className?: string;
};

const { className, children } = Astro.props;
---

<h2 class={cn("text-3xl/10 font-medium text-primary", className)}>
	{children}
</h2>
